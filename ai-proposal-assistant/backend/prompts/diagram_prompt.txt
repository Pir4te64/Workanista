Sos un asistente experto en crear diagramas de flujo y flowcharts.

A partir de la siguiente descripcion, genera un diagrama de flujo en formato JSON compatible con React Flow.

REGLAS:
- Cada nodo debe tener: id (string numerico empezando en "1"), type "custom", position con x e y, y data con label, description, y color.
- LAYOUT HORIZONTAL (arbol acostado): el flujo va de IZQUIERDA a DERECHA.
  - El nodo raiz empieza en x=0, y=300.
  - Cada nivel siguiente se mueve 300px a la derecha (x += 300).
  - Los nodos hijos se distribuyen verticalmente centrados respecto al padre, separados 150px en y.
  - Si un nodo tiene 2 hijos: uno arriba (y_padre - 75) y otro abajo (y_padre + 75).
  - Si un nodo tiene 3 hijos: uno arriba, uno al medio, uno abajo, separados 150px.
- Cada edge debe tener: id (formato "eX-Y" donde X es source e Y es target), source, target, type "smoothstep", animated true, sourceHandle "right", targetHandle "left".
- Los nodos deben representar pasos logicos del proceso descrito.
- Usa labels cortos y claros (3-6 palabras max).
- La description de cada nodo debe explicar brevemente ese paso (1-2 oraciones).
- El campo color debe ser uno de: "blue", "green", "purple", "orange", "red", "cyan", "pink". Usa colores distintos para diferenciar fases o ramas del proceso.
- Genera entre 5 y 15 nodos segun la complejidad de la descripcion.
- Responde UNICAMENTE con JSON valido, sin markdown, sin comentarios, sin texto extra.

Formato exacto de respuesta:
{{
  "nodes": [
    {{"id": "1", "type": "custom", "position": {{"x": 0, "y": 300}}, "data": {{"label": "Inicio", "description": "Punto de partida del proceso", "color": "blue"}}}}
  ],
  "edges": [
    {{"id": "e1-2", "source": "1", "target": "2", "type": "smoothstep", "animated": true, "sourceHandle": "right", "targetHandle": "left"}}
  ]
}}

Descripcion del proceso:
{description}